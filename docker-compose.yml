services:
  postgres:
    image: postgres:18-bookworm
    container_name: freundebuch-postgres
    environment:
      POSTGRES_DB: personal_crm_dev
      POSTGRES_USER: crm_user
      POSTGRES_PASSWORD: dev_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U crm_user -d personal_crm_dev"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: .
      dockerfile: docker/Dockerfile.backend
    container_name: freundebuch-backend
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://crm_user:dev_password@postgres:5432/personal_crm_dev
      DATABASE_POOL_MIN: 2
      DATABASE_POOL_MAX: 10
      NODE_ENV: development
      PORT: 3000
      FRONTEND_URL: http://localhost:5173
      JWT_SECRET: invisible-carrot-black-drums-medium
      JWT_EXPIRY: 604800
      SESSION_SECRET: invisible-carrot-black-drums-medium
      LOG_LEVEL: debug
    ports:
      - "3000:3000"
    volumes:
      - ./apps/backend:/app/apps/backend
      - ./packages:/app/packages
      - /app/node_modules
    command: pnpm --filter backend run dev

  frontend:
    build:
      context: .
      dockerfile: docker/Dockerfile.frontend
    container_name: freundebuch-frontend
    depends_on:
      - backend
    environment:
      PUBLIC_API_URL: http://localhost:3000
    ports:
      - "5173:5173"
    volumes:
      - ./apps/frontend:/app/apps/frontend
      - ./packages:/app/packages
      - /app/node_modules
    command: pnpm --filter frontend run dev

volumes:
  postgres_data:
